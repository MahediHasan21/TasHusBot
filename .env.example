# Copy this file to a `.env` in the project root and fill in the values.
# Do NOT commit your real credentials. Add `.env` to .gitignore (already included).

# Groq API key for ChatGroq
GROQ_API_KEY=

# Any other environment variables your app needs can go here
